#include <stdio.h>

int main() {
	    const char *message = "y";  // 반복해서 출력할 문자열
					
					     while (1) {
					             printf("%s\n", message);
					                 }
					
					                     return 0;
					                     }
					
